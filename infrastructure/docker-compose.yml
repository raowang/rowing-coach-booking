version: '3.8'

services:
  # Directus - Headless CMS / API Layer
  directus:
    image: directus/directus:latest
    container_name: rowing-directus
    environment:
      DB_CLIENT: sqlite
      DB_FILENAME: /data/rowing.db
      SECRET: ${DIRECTUS_SECRET:-your-secret-key}
      ADMIN_EMAIL: admin@rowing.com
      ADMIN_PASSWORD: ${DIRECTUS_ADMIN_PASSWORD:-admin123}
    ports:
      - "8055:8055"
    volumes:
      - sqlite_data:/data
      - directus_uploads:/directus/uploads
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8055/server/health"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  sqlite_data:
  directus_uploads:
